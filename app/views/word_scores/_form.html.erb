<%= form_for(@word_score) do |f| %>
  <% if @word_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@word_score.errors.count, "error") %> prohibited this word_score from being saved:</h2>

      <ul>
      <% @word_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br>
    <%= f.text_field :word %>
  </div>
  <!--<div class="field">-->
  <!--  <%= f.label :score %><br>-->
  <!--  <%= f.number_field :score %>-->
  <!--</div>-->
  <div class="actions">
    <%= f.submit "Calculate Word Score" %>
  </div>
<% end %>
